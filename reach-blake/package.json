{
  "name": "reach-blake",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "site": "python3 -m http.server 8000 --bind 127.0.0.1 --directory ../docs",
    "test": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "prettier": "^3.9.6",
    "vitest": "~4.1.0",
    "wrangler": "^4.127.0"
  }
}
